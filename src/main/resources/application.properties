server.port=8085

spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=bggdb

server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain,text/css,application/javascript

#Commit to Git
# Add system.properties files in project root and insert <java.runtime.version=18>
# git init
# git remote add origin <git repo link>
# git add *
# git commit -m "addmessagehere"
# git push -u origin master

#Deploying to railway
# railway login
# railway init --> create the project name
# railway up --> generate the domain and add the environment variables
#To update the application
# railway up